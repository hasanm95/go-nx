root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd"
  bin = "./tmp/main"
  full_bin = "./tmp/main --config config.yml"
  include_ext = ["go", "yml"]
  exclude_dir = ["tmp", "vendor"]
  delay = 1000

[log]
  time = true